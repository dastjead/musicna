# web

musicna 웹 UI. **Phase 5에서 구현.**

api(FastAPI)만 호출하는 순수 클라이언트 — macOS 지식 없음. 추후 iOS 앱과 동일한 API 계약(OpenAPI)을 공유한다.

예정 화면: 라이브러리 브라우저(곡 목록·검색), 곡 상세(구조 타임라인, 코드 진행, 피아노롤, 무드), 통계(무드 분포), 실시간 뷰(Phase 6, WebSocket).
